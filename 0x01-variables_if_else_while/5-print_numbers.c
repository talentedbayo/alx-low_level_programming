#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Prints the decimal numbers
 * Return: Always ) (Success)
 */

int main(void)
{
	int i;
	for (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");
	return (0);
}
